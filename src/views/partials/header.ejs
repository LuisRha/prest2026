<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof titulo !== 'undefined' ? titulo : 'Préstamos' %></title>
  <link rel="stylesheet" href="/css/estilos.css" />
</head>
<body>
  <% if (typeof sesion !== 'undefined' && sesion) { %>
  <nav class="navbar">
    <div class="nav-marca">💰 Préstamos</div>
    <div class="nav-links">
      <a href="/">Inicio</a>
      <a href="/clientes">Clientes</a>
      <a href="/prestamos">Préstamos</a>
      <a href="/links">Links registro</a>
      <form action="/logout" method="POST" class="nav-logout">
        <button type="submit" class="btn-link">Salir (<%= sesion.usuario %>)</button>
      </form>
    </div>
  </nav>
  <% } %>
  <main class="contenedor">
